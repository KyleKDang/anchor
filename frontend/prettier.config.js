// Everything else is Prettier's default, which the files already followed. The width is
// the one the codebase was written at: measured over src/ and e2e/ when the formatter
// arrived, 100 changed a third as many lines as 90 and fewer than 110 (#106).
export default {
  printWidth: 100,
};
